# 通义万相 T2I API 配置
# 从阿里云百炼控制台获取: https://bailian.console.aliyun.com/

# DashScope API Key（必需）
# 获取方式：登录阿里云百炼控制台 -> API-KEY 管理
DASHSCOPE_API_KEY=sk-11111

# API 端点（可选，使用默认值）
# WAN_T2I_ENDPOINT=https://dashscope.aliyuncs.com/api/v1/services/aigc/text2image/generation

# 模型名称（可选，默认 wan2.5-t2i-preview）
# WAN_T2I_MODEL=wan2.5-t2i-preview

# 请求超时时间（秒，可选，默认 120）
# WAN_T2I_TIMEOUT=120

# 最大重试次数（可选，默认 3）
# WAN_T2I_MAX_RETRIES=3

# 默认图像尺寸（可选，默认 1280*1280）
# 注意：总像素必须在 768*768 到 1440*1440 之间，宽高比必须在 1:4 到 4:1 之间
# WAN_T2I_DEFAULT_SIZE=1280*1280

# 默认生成图像数量（可选，默认 1）
# WAN_T2I_DEFAULT_N=1
