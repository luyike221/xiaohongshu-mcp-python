# 1. 安装依赖

pnpm install

# 2. 构建 UI

pnpm ui:build

# 3. 构建项目（生成 dist/ 目录）

pnpm build

# 4. 运行 onboarding 向导（引导配置网关、工作区、频道、技能等）

pnpm openclaw onboard --install-daemon

# 5. 开发循环（源码/配置变更时自动重载）

pnpm gateway:watch

# 6. 获取可用的 dashboard URL（推荐）

pnpm openclaw dashboard --no-open

如果你发现 token 根本没配置/不工作：先生成
pnpm openclaw doctor --generate-gateway-token
然后再执行一次：
pnpm openclaw dashboard --no-open
